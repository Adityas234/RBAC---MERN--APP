# MERN RBAC Application

A full-stack Role-Based Access Control system built with MongoDB, Express, React, and Node.js.

## Features
- Admin, User, Viewer roles
- JWT authentication
- Role-based route protection
- Audit logging
- Docker setup for backend, frontend, and MongoDB

## Local Setup
```bash
cd backend && npm install
cd ../frontend && npm install
npm run dev
